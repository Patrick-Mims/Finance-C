#include "finsftp.h"

/*
void sftp_init(int argc, char **argv)
{
login_t *login = NULL;
struct FINSFTP finsftp;
struct FINSFTP *fin;

fin = &finsftp;

if(libssh2_init(0))
{
    fprintf(stderr, "Error: libssh2 failed (%d)\n", fin->rc);
    exit(EXIT_FAILURE);
}

if((fin->sock = socket(AF_INET, SOCK_STREAM, 0)) == NULL)
{
    fprintf(stderr, "Error: libssh2 socket failure");
    exit(EXIT_FAILURE);
}
*/

/*
sin.sin_family = AF_INET;
sin.sin_port = htons(22);
sin.sin_addr.s_addr = fin->host;
}
*/